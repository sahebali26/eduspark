# Edu SmartSpark .htaccess

# Ensure XML sitemaps are served with correct content type
AddType application/xml .xml

# Prevent directory listing
Options -Indexes

# Optional: force HTTPS (uncomment if SSL enabled)
# RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
